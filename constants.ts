import { join } from 'node:path';

export const INPUT_DIR = `./data/in`;
export const RENAMED_DIR = `./data/renamed`;
export const OUTPUT_ROOT_DIR = `./data/out`;
export const OUTPUT_DIR = join(OUTPUT_ROOT_DIR, 'cards');